import React from 'react';
import MainTemplate from 'templates/MainTemplate';

const SettingsPage = () => (
    <MainTemplate>
        <div>
            SettingsPage
        </div>
    </MainTemplate>
);

export default SettingsPage;