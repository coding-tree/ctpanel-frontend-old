import React from 'react';
import {DescriptionWrapper} from 'styledComponents/LastMeet';

const LastMeetDescription = ({description}) => {
  return <DescriptionWrapper>{description}</DescriptionWrapper>;
};

export default LastMeetDescription;
