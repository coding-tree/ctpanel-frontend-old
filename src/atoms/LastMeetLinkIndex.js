import React from 'react';
import {IndexWrapper} from 'styledComponents/LastMeet';

const LastMeetLinkIndex = ({index}) => {
  return <IndexWrapper>#{index + 1}</IndexWrapper>;
};

export default LastMeetLinkIndex;
