import React from 'react';
import {TitleWrapper} from 'styledComponents/LastMeet';

const LastMeetTitle = ({children}) => {
  return <TitleWrapper>{children}</TitleWrapper>;
};

export default LastMeetTitle;
